# 复制本文件为同目录下的 .env，并填入真实值（.env 已被 gitignore，不会进仓库）
# 注意：密码里的 @ 必须写成 %40

# 你的外部 Postgres 连接串（这里用你给的 42.193.104.173:5432 / admin / sdl@admin）
DATABASE_URL=postgres://admin:sdl%40admin@42.193.104.173:5432/baoyan?sslmode=disable

# 可选：设置了则前端 POST 进度必须带 Authorization: Bearer <token>
API_TOKEN=
